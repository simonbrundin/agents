apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: auth
  namespace: software-agent
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: auth
  dataFrom:
    - extract:
        key: prod/software-agent/auth
